{
  "name": "challenges",
  "private": true,
  "license": "MIT",
  "scripts": {
    "format": "prettier --write ./**/*.js",
    "lint": "prettier --check ./**/*.js",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "jest": "^24.9.0",
    "mocha": "^5.0.3",
    "prettier": "^1.19.1"
  }
}
